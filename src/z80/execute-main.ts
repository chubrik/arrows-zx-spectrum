import { get, get16, set, setReg } from '../common/utils';
import { executeBit } from './execute-bit';
import { executeBitXYd } from './execute-bit-xyd';
import { executeMisc } from './execute-misc';
import { fc, FH, fo, fs, fz, HLT, hlt, IFF1, IFF2, setHLT, setIFF1, setIFF2 } from './flags';
import { EX_AF_AF, EX_DE_HL, EX_sp_HL, EXX } from './op/op-exchange';
import { IN_A_n, OUT_n_A } from './op/op-io';
import { DJNZ_e, JR_e } from './op/op-jump';
import { ld16, ld16Next, ldReg16 } from './op/op-load-16bit';
import { addHLXY, decReg16, incReg16 } from './op/op-math-16bit';
import { add, cp, dec, decReg, inc, incReg, logic, sub } from './op/op-math-8bit';
import { CCF, CPL, DAA, SCF } from './op/op-math-etc';
import { RLA, RLCA, RRA, RRCA } from './op/op-shift';
import { call88, callNext16, pop16, popAF, push16, pushAF } from './op/op-stack';
import { A, B, BC, C, D, DE, E, H, HL, HLXY, HXY, IX, IY, L, LXY, PC, PCh, PCl, setEIDelay, setHLXY, SP, SPh, SPl } from './registers';
import { addPC, getHLXYd, next, next16, nop, refresh, setPCNext16 } from './utils';

export function executeMain() {
  refresh();
  if (hlt) return;
  const op = next();
  opsMain[op]();
}

const opsMain = [
  /* 00 NOP        */ nop,
  /* 01 LD BC,nn   */ () => ld16Next(BC),
  /* 02 LD (BC),A  */ () => set(get16(BC), get(A)),
  /* 03 INC BC     */ () => incReg16(BC),
  /* 04 INC B      */ () => incReg(B),
  /* 05 DEC B      */ () => decReg(B),
  /* 06 LD B,n     */ () => setReg(B, next()),
  /* 07 RLCA       */ RLCA,
  /* 08 EX AF,AF'  */ EX_AF_AF,
  /* 09 ADD HL,BC  */ () => addHLXY(BC),
  /* 0A LD A,(BC)  */ () => setReg(A, get(get16(BC))),
  /* 0B DEC BC     */ () => decReg16(BC),
  /* 0C INC C      */ () => incReg(C),
  /* 0D DEC C      */ () => decReg(C),
  /* 0E LD C,n     */ () => setReg(C, next()),
  /* 0F RRCA       */ RRCA,

  /* 10 DJNZ e     */ DJNZ_e,
  /* 11 LD DE,nn   */ () => ld16Next(DE),
  /* 12 LD (DE),A  */ () => set(get16(DE), get(A)),
  /* 13 INC DE     */ () => incReg16(DE),
  /* 14 INC D      */ () => incReg(D),
  /* 15 DEC D      */ () => decReg(D),
  /* 16 LD D,n     */ () => setReg(D, next()),
  /* 17 RLA        */ RLA,
  /* 18 JR e       */ JR_e,
  /* 19 ADD HL,DE  */ () => addHLXY(DE),
  /* 1A LD A,(DE)  */ () => setReg(A, get(get16(DE))),
  /* 1B DEC DE     */ () => decReg16(DE),
  /* 1C INC E      */ () => incReg(E),
  /* 1D DEC E      */ () => decReg(E),
  /* 1E LD E,n     */ () => setReg(E, next()),
  /* 1F RRA        */ RRA,

  /* 20 JR NZ,e    */ () => fz ? addPC(1) : JR_e(),
  /* 21 LD HL,nn   */ () => ld16Next(HLXY),
  /* 22 LD (nn),HL */ () => ld16(next16(), HLXY),
  /* 23 INC HL     */ () => incReg16(HLXY),
  /* 24 INC H      */ () => incReg(HXY),
  /* 25 DEC H      */ () => decReg(HXY),
  /* 26 LD H,n     */ () => setReg(HXY, next()),
  /* 27 DAA        */ DAA,
  /* 28 JR Z,e     */ () => fz ? JR_e() : addPC(1),
  /* 29 ADD HL,HL  */ () => addHLXY(HLXY),
  /* 2A LD HL,(nn) */ () => ldReg16(HLXY, next16()),
  /* 2B DEC HL     */ () => decReg16(HLXY),
  /* 2C INC L      */ () => incReg(LXY),
  /* 2D DEC L      */ () => decReg(LXY),
  /* 2E LD L,n     */ () => setReg(LXY, next()),
  /* 2F CPL        */ CPL,

  /* 30 JR NC,e    */ () => fc ? addPC(1) : JR_e(),
  /* 31 LD SP,nn   */ () => ld16Next(SP),
  /* 32 LD (nn),A  */ () => set(next16(), get(A)),
  /* 33 INC SP     */ () => incReg16(SP),
  /* 34 INC (HL)   */ () => inc(getHLXYd()),
  /* 35 DEC (HL)   */ () => dec(getHLXYd()),
  /* 36 LD (HL),n  */ () => set(getHLXYd(), next()),
  /* 37 SCF        */ SCF,
  /* 38 JR C,e     */ () => fc ? JR_e() : addPC(1),
  /* 39 ADD HL,SP  */ () => addHLXY(SP),
  /* 3A LD A,(nn)  */ () => setReg(A, get(next16())),
  /* 3B DEC SP     */ () => decReg16(SP),
  /* 3C INC A      */ () => incReg(A),
  /* 3D DEC A      */ () => decReg(A),
  /* 3E LD A,n     */ () => setReg(A, next()),
  /* 3F CCF        */ CCF,

  /* 40 LD B,B     */ nop,
  /* 41 LD B,C     */ () => setReg(B, get(C)),
  /* 42 LD B,D     */ () => setReg(B, get(D)),
  /* 43 LD B,E     */ () => setReg(B, get(E)),
  /* 44 LD B,H     */ () => setReg(B, get(HXY)),
  /* 45 LD B,L     */ () => setReg(B, get(LXY)),
  /* 46 LD B,(HL)  */ () => setReg(B, get(getHLXYd())),
  /* 47 LD B,A     */ () => setReg(B, get(A)),
  /* 48 LD C,B     */ () => setReg(C, get(B)),
  /* 49 LD C,C     */ nop,
  /* 4A LD C,D     */ () => setReg(C, get(D)),
  /* 4B LD C,E     */ () => setReg(C, get(E)),
  /* 4C LD C,H     */ () => setReg(C, get(HXY)),
  /* 4D LD C,L     */ () => setReg(C, get(LXY)),
  /* 4E LD C,(HL)  */ () => setReg(C, get(getHLXYd())),
  /* 4F LD C,A     */ () => setReg(C, get(A)),

  /* 50 LD D,B     */ () => setReg(D, get(B)),
  /* 51 LD D,C     */ () => setReg(D, get(C)),
  /* 52 LD D,D     */ nop,
  /* 53 LD D,E     */ () => setReg(D, get(E)),
  /* 54 LD D,H     */ () => setReg(D, get(HXY)),
  /* 55 LD D,L     */ () => setReg(D, get(LXY)),
  /* 56 LD D,(HL)  */ () => setReg(D, get(getHLXYd())),
  /* 57 LD D,A     */ () => setReg(D, get(A)),
  /* 58 LD E,B     */ () => setReg(E, get(B)),
  /* 59 LD E,C     */ () => setReg(E, get(C)),
  /* 5A LD E,D     */ () => setReg(E, get(D)),
  /* 5B LD E,E     */ nop,
  /* 5C LD E,H     */ () => setReg(E, get(HXY)),
  /* 5D LD E,L     */ () => setReg(E, get(LXY)),
  /* 5E LD E,(HL)  */ () => setReg(E, get(getHLXYd())),
  /* 5F LD E,A     */ () => setReg(E, get(A)),

  /* 60 LD H,B     */ () => setReg(HXY, get(B)),
  /* 61 LD H,C     */ () => setReg(HXY, get(C)),
  /* 62 LD H,D     */ () => setReg(HXY, get(D)),
  /* 63 LD H,E     */ () => setReg(HXY, get(E)),
  /* 64 LD H,H     */ nop,
  /* 65 LD H,L     */ () => setReg(HXY, get(LXY)),
  /* 66 LD H,(HL)  */ () => setReg(H, get(getHLXYd())),
  /* 67 LD H,A     */ () => setReg(HXY, get(A)),
  /* 68 LD L,B     */ () => setReg(LXY, get(B)),
  /* 69 LD L,C     */ () => setReg(LXY, get(C)),
  /* 6A LD L,D     */ () => setReg(LXY, get(D)),
  /* 6B LD L,E     */ () => setReg(LXY, get(E)),
  /* 6C LD L,H     */ () => setReg(LXY, get(HXY)),
  /* 6D LD L,L     */ nop,
  /* 6E LD L,(HL)  */ () => setReg(L, get(getHLXYd())),
  /* 6F LD L,A     */ () => setReg(LXY, get(A)),

  /* 70 LD (HL),B  */ () => set(getHLXYd(), get(B)),
  /* 71 LD (HL),C  */ () => set(getHLXYd(), get(C)),
  /* 72 LD (HL),D  */ () => set(getHLXYd(), get(D)),
  /* 73 LD (HL),E  */ () => set(getHLXYd(), get(E)),
  /* 74 LD (HL),H  */ () => set(getHLXYd(), get(H)),
  /* 75 LD (HL),L  */ () => set(getHLXYd(), get(L)),
  /* 76 HALT       */ () => { setHLT(HLT); addPC(-1); },
  /* 77 LD (HL),A  */ () => set(getHLXYd(), get(A)),
  /* 78 LD A,B     */ () => setReg(A, get(B)),
  /* 79 LD A,C     */ () => setReg(A, get(C)),
  /* 7A LD A,D     */ () => setReg(A, get(D)),
  /* 7B LD A,E     */ () => setReg(A, get(E)),
  /* 7C LD A,H     */ () => setReg(A, get(HXY)),
  /* 7D LD A,L     */ () => setReg(A, get(LXY)),
  /* 7E LD A,(HL)  */ () => setReg(A, get(getHLXYd())),
  /* 7F LD A,A     */ nop,

  /* 80 ADD A,B    */ () => add(get(B)),
  /* 81 ADD A,C    */ () => add(get(C)),
  /* 82 ADD A,D    */ () => add(get(D)),
  /* 83 ADD A,E    */ () => add(get(E)),
  /* 84 ADD A,H    */ () => add(get(HXY)),
  /* 85 ADD A,L    */ () => add(get(LXY)),
  /* 86 ADD A,(HL) */ () => add(get(getHLXYd())),
  /* 87 ADD A,A    */ () => add(get(A)),
  /* 88 ADC A,B    */ () => add(get(B), fc),
  /* 89 ADC A,C    */ () => add(get(C), fc),
  /* 8A ADC A,D    */ () => add(get(D), fc),
  /* 8B ADC A,E    */ () => add(get(E), fc),
  /* 8C ADC A,H    */ () => add(get(HXY), fc),
  /* 8D ADC A,L    */ () => add(get(LXY), fc),
  /* 8E ADC A,(HL) */ () => add(get(getHLXYd()), fc),
  /* 8F ADC A,A    */ () => add(get(A), fc),

  /* 90 SUB B      */ () => sub(get(B)),
  /* 91 SUB C      */ () => sub(get(C)),
  /* 92 SUB D      */ () => sub(get(D)),
  /* 93 SUB E      */ () => sub(get(E)),
  /* 94 SUB H      */ () => sub(get(HXY)),
  /* 95 SUB L      */ () => sub(get(LXY)),
  /* 96 SUB (HL)   */ () => sub(get(getHLXYd())),
  /* 97 SUB A      */ () => sub(get(A)),
  /* 98 SBC A,B    */ () => sub(get(B), fc),
  /* 99 SBC A,C    */ () => sub(get(C), fc),
  /* 9A SBC A,D    */ () => sub(get(D), fc),
  /* 9B SBC A,E    */ () => sub(get(E), fc),
  /* 9C SBC A,H    */ () => sub(get(HXY), fc),
  /* 9D SBC A,L    */ () => sub(get(LXY), fc),
  /* 9E SBC A,(HL) */ () => sub(get(getHLXYd()), fc),
  /* 9F SBC A,A    */ () => sub(get(A), fc),

  /* A0 AND B      */ () => logic(get(A) & get(B), FH),
  /* A1 AND C      */ () => logic(get(A) & get(C), FH),
  /* A2 AND D      */ () => logic(get(A) & get(D), FH),
  /* A3 AND E      */ () => logic(get(A) & get(E), FH),
  /* A4 AND H      */ () => logic(get(A) & get(HXY), FH),
  /* A5 AND L      */ () => logic(get(A) & get(LXY), FH),
  /* A6 AND (HL)   */ () => logic(get(A) & get(getHLXYd()), FH),
  /* A7 AND A      */ () => logic(get(A), FH),
  /* A8 XOR B      */ () => logic(get(A) ^ get(B)),
  /* A9 XOR C      */ () => logic(get(A) ^ get(C)),
  /* AA XOR D      */ () => logic(get(A) ^ get(D)),
  /* AB XOR E      */ () => logic(get(A) ^ get(E)),
  /* AC XOR H      */ () => logic(get(A) ^ get(HXY)),
  /* AD XOR L      */ () => logic(get(A) ^ get(LXY)),
  /* AE XOR (HL)   */ () => logic(get(A) ^ get(getHLXYd())),
  /* AF XOR A      */ () => logic(0),

  /* B0 OR B       */ () => logic(get(A) | get(B)),
  /* B1 OR C       */ () => logic(get(A) | get(C)),
  /* B2 OR D       */ () => logic(get(A) | get(D)),
  /* B3 OR E       */ () => logic(get(A) | get(E)),
  /* B4 OR H       */ () => logic(get(A) | get(HXY)),
  /* B5 OR L       */ () => logic(get(A) | get(LXY)),
  /* B6 OR (HL)    */ () => logic(get(A) | get(getHLXYd())),
  /* B7 OR A       */ () => logic(get(A)),
  /* B8 CP B       */ () => cp(get(B)),
  /* B9 CP C       */ () => cp(get(C)),
  /* BA CP D       */ () => cp(get(D)),
  /* BB CP E       */ () => cp(get(E)),
  /* BC CP H       */ () => cp(get(HXY)),
  /* BD CP L       */ () => cp(get(LXY)),
  /* BE CP (HL)    */ () => cp(get(getHLXYd())),
  /* BF CP A       */ () => cp(get(A)),

  /* C0 RET NZ     */ () => fz ? {} : pop16(PC),
  /* C1 POP BC     */ () => pop16(BC),
  /* C2 JP NZ,nn   */ () => fz ? addPC(2) : setPCNext16(),
  /* C3 JP nn      */ () => setPCNext16(),
  /* C4 CALL NZ,nn */ () => fz ? addPC(2) : callNext16(),
  /* C5 PUSH BC    */ () => push16(BC),
  /* C6 ADD A,n    */ () => add(next()),
  /* C7 CALL 00h   */ () => call88(0x00),
  /* C8 RET Z      */ () => fz ? pop16(PC) : {},
  /* C9 RET        */ () => pop16(PC),
  /* CA JP Z,nn    */ () => fz ? setPCNext16() : addPC(2),
  /* CB -- BIT --- */ () => HLXY === HL ? executeBit() : executeBitXYd(),
  /* CC CALL Z,nn  */ () => fz ? callNext16() : addPC(2),
  /* CD CALL nn    */ () => callNext16(),
  /* CE ADC A,n    */ () => add(next(), fc),
  /* CF CALL 08h   */ () => call88(0x08),

  /* D0 RET NC     */ () => fc ? {} : pop16(PC),
  /* D1 POP DE     */ () => pop16(DE),
  /* D2 JP NC,nn   */ () => fc ? addPC(2) : setPCNext16(),
  /* D3 OUT (n),A  */ OUT_n_A,
  /* D4 CALL NC,nn */ () => fc ? addPC(2) : callNext16(),
  /* D5 PUSH DE    */ () => push16(DE),
  /* D6 SUB A,n    */ () => sub(next()),
  /* D7 CALL 10h   */ () => call88(0x10),
  /* D8 RET C      */ () => fc ? pop16(PC) : {},
  /* D9 EXX        */ EXX,
  /* DA JP C,nn    */ () => fc ? setPCNext16() : addPC(2),
  /* DB IN A,(n)   */ IN_A_n,
  /* DC CALL C,nn  */ () => fc ? callNext16() : addPC(2),
  /* DD --- IX --- */ () => { setHLXY(IX); executeMain(); setHLXY(HL); },
  /* DE SBC A,n    */ () => sub(next(), fc),
  /* DF CALL 18h   */ () => call88(0x18),

  /* E0 RET PO     */ () => fo ? {} : pop16(PC),
  /* E1 POP HL     */ () => pop16(HLXY),
  /* E2 JP PO,nn   */ () => fo ? addPC(2) : setPCNext16(),
  /* E3 EX (SP),HL */ EX_sp_HL,
  /* E4 CALL PO,nn */ () => fo ? addPC(2) : callNext16(),
  /* E5 PUSH HL    */ () => push16(HLXY),
  /* E6 AND n      */ () => logic(get(A) & next(), FH),
  /* E7 CALL 20h   */ () => call88(0x20),
  /* E8 RET PE     */ () => fo ? pop16(PC) : {},
  /* E9 JP (HL)    */ () => { setReg(PCl, get(LXY)); setReg(PCh, get(HXY)); },
  /* EA JP PE,nn   */ () => fo ? setPCNext16() : addPC(2),
  /* EB EX DE,HL   */ EX_DE_HL,
  /* EC CALL PE,nn */ () => fo ? callNext16() : addPC(2),
  /* ED -- MISC -- */ executeMisc,
  /* EE XOR n      */ () => logic(get(A) ^ next()),
  /* EF CALL 28h   */ () => call88(0x28),

  /* F0 RET P      */ () => fs ? {} : pop16(PC),
  /* F1 POP AF     */ popAF,
  /* F2 JP P,nn    */ () => fs ? addPC(2) : setPCNext16(),
  /* F3 DI         */ () => { setIFF1(0); setIFF2(0); },
  /* F4 CALL P,nn  */ () => fs ? addPC(2) : callNext16(),
  /* F5 PUSH AF    */ pushAF,
  /* F6 OR n       */ () => logic(get(A) | next()),
  /* F7 RST 30h    */ () => call88(0x30),
  /* F8 RET M      */ () => fs ? pop16(PC) : {},
  /* F9 LD SP,HL   */ () => { setReg(SPl, get(LXY)); setReg(SPh, get(HXY)); },
  /* FA JP M,nn    */ () => fs ? setPCNext16() : addPC(2),
  /* FB EI         */ () => { setIFF1(IFF1); setIFF2(IFF2); setEIDelay(1); },
  /* FC CALL M,nn  */ () => fs ? callNext16() : addPC(2),
  /* FD --- IY --- */ () => { setHLXY(IY); executeMain(); setHLXY(HL); },
  /* FE CP n       */ () => cp(next()),
  /* FF CALL 38h   */ () => call88(0x38),
];
