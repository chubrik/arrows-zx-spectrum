{
  "private": true,
  "name": "arrows-zx-spectrum",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "node build/build.ts",
    "dev": "node build/build.ts --cpuOnly",
    "test": "vitest run",
    "prebuild": "tsc -p tsconfig.json",
    "pretest": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "acorn": "^8.18.0",
    "esbuild": "^0.28.2",
    "quickjs-emscripten": "^0.32.0",
    "terser": "^5.51.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
