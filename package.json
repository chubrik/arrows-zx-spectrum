{
  "private": true,
  "name": "arrows-zx-spectrum",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types build/build.ts",
    "dev": "node --experimental-strip-types build/build.ts --cpu",
    "test": "vitest run",
    "prebuild": "tsc -p tsconfig.json",
    "pretest": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "esbuild": "^0.28.2",
    "terser": "^5.51.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
